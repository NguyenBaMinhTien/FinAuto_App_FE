name: finauto_app_fe
description: "Ứng dụng quản lý thu chi tiện lợi dành cho chủ cửa hàng"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.1.0
  intl: ^0.19.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


